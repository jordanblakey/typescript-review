{
  "name": "learn-you-typescript",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "tsc types.ts --watch --target ES5",
    "lint": "tslint -c tslint.json 'src/**/*.ts"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "tslint": "^5.10.0",
    "typescript": "^2.8.3"
  },
  "devDependencies": {
    "typedoc": "^0.11.1"
  }
}
